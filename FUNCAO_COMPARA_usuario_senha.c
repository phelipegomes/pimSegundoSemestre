#include "cabecalho.h"
login log[100];

void FUNCAO_COMPARA_usuario_senha()
{
	if ((strcmp(usuario_digitado, log[i].usuario) == 0) && (strcmp(senha_digitado, log[i].senha) == 0))
	{
		ret = 1;
	}
	return ret;
}